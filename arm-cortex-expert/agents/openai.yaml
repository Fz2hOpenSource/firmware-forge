interface:
  display_name: "ARM Cortex Expert"
  short_description: "Cortex-M drivers, RTOS, DSP, storage and debugging"
  default_prompt: "Use $arm-cortex-expert to implement or diagnose the requested Cortex-M firmware task, using project evidence and the relevant driver, RTOS, DSP, storage or network checks."

policy:
  allow_implicit_invocation: true
