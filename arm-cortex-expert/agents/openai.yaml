interface:
  display_name: "ARM Cortex Expert"
  short_description: "Cortex-M firmware: DMA/ISR/cache/RTOS/LwIP boundaries"
  default_prompt: "Use $arm-cortex-expert for Cortex-M firmware design/review using the complexity ladder, hard safety boundaries, DMA, ISR priorities, cache coherency, RTOS/CMSIS boundaries, LwIP/Ethernet integration, and buffer ownership."

policy:
  allow_implicit_invocation: true