# Maintaining this skill

Use `SKILL.md` as the single maintained entry for scope, source priorities, read
order and tool-path resolution. Read the references relevant to the change rather
than duplicating their rules in this file. Preserve user changes and existing
platform/tool capabilities when synchronizing deployments; do not overwrite a
different tool implementation as part of a documentation-only update.
