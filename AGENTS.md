# 项目协作约定

本仓库维护三个嵌入式技能及配套工具。需求先行，采用当前最小足够方案，后续局部扩展。

设计取舍见 [设计理念](docs/design-philosophy.md)，面向贡献者的步骤见 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 使用技能

当前提供 Codex 与 DSH 两种使用入口。三个技能位于仓库顶层：`embedded-protocol-designer/`、`arm-cortex-expert/`、`embedded-test-engineer/`。
按任务读取对应 `SKILL.md`，无需每次加载全部技能。安装与更新见 [使用指南](docs/workbench-usage.md)，采用实际 loader 报告的路径。
`install.ps1` / `install.bat` 仅用于 DSH 工作台；在 Codex 使用技能按指南复制完整技能目录。

## 修改

- 先查看需求、相关文件和 Git 差异，明确本次验收条件。讨论阶段不直接修改。
- 保留用户已有改动；不顺便重构无关内容。新增状态、抽象或依赖应有当前需求依据。
- 工程方法维护在相应 `SKILL.md` 和参考文档，本文件只约束仓库协作，不重复技能细则。
- 仓库是共用技能的维护源。同步安装副本时按文件检查差异、备份，保留宿主扩展和不同版本的工具；不整目录覆盖。

## 验证

- 按风险检查：技能文档检查格式、引用和规则一致性；工具行为修改带相关测试和用法说明。
- 共用检查：`python -X utf8 scripts/validate_skills.py`。它只是浅层检查；YAML 修改另做语法解析，不执行其中的 JS 标签。
- 状态检查器修改运行 `python -m unittest discover -s embedded-test-engineer/tests`；其它工具运行其相关测试。不强制每次全量测试。
- 已通过且内容未变的检查可复用，注明范围；未执行的实机或 AI 行为验证不得写为通过。

## Git

- 用户要求提交时，在当前工作分支完成本地提交；不强制另建分支。推送、发布按单独授权执行，已有授权不重复询问。
- 一个提交表达一个可独立理解、验证和回退的目的。相关代码、测试、说明放一起；小改动不强制拆分。
- 提交前核对暂存差异并运行 `git diff --cached --check`，按明确文件或改动块暂存，避免混入无关内容、本机配置和会话记录。
- 标题简短说明目的，可沿用 `docs:`、`fix:`、`feat:`、`chore:`；正文仅补充必要原因、验证和限制。
- 完成后报告提交编号及剩余改动。不自动改写已有历史，也不要求额外的长篇提交报告。
