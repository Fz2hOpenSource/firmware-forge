interface:
  display_name: "Embedded Test Engineer"
  short_description: "固件单元测试、驱动隔离、测量回放、精度回归与时序验证"
  default_prompt: "使用 $embedded-test-engineer 为当前固件变更选择必要的单元、驱动、回放或时序验证，明确验收依据并说明目标平台验证缺口。"

policy:
  allow_implicit_invocation: true
