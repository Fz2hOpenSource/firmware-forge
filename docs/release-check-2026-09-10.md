# 2026-09-10 发布前检查

检查基线为 `fa9a6fb`，本次调整后的源码在独立暂存目录验证。当前提供 Codex 技能与 DSH 工作台两种入口，其它宿主适配留待后续需求。

## 本轮变化

- 为 AI 协作者补充三个技能的位置及 Codex / DSH 安装导航。
- 三个技能均显式允许隐式调用，保持原默认行为；补齐固件状态转移与恢复的范围说明。
- 架构说明纳入状态图检查器及其证据边界，英文文档补齐工程昵称示例。
- 修复 MDK 插件外层 600 秒先于 UV4 内层 900 秒到期的问题：明确传入 900 秒内层等待，外层采用 960 秒；外层中断报告结果不确定，保留诊断且不自动重试。

## 已执行

Windows，Python 3.14.2，Node.js 24.13.0：

| 检查 | 结果 |
|---|---|
| 状态检查器 unittest | 18 项通过 |
| stm32cli unittest（构造数据库） | 23 项通过 |
| MDK 插件回归（替身进程） | 4 项通过；旧实现能被新增时限及中断语义用例检出 |
| 三技能格式、入口与 UI 元数据 | 通过；隐式调用策略一致 |
| 仓库共用校验器 | 0 失败、0 警告 |
| YAML / PowerShell / JavaScript | 语法检查通过；未执行 YAML 中的 JS 标签 |
| 本地文档链接、核心命令和安装示例 | 检查通过；故意失败的状态图得到预期退出码 1 |
| Codex 首次安装示例 | 隔离目录完整复制；重复安装拒绝覆盖已有定制 |
| DSH 安装及更新 | 隔离 DSH_HOME 复制模式通过；保留本机配置及日志，受管技能按文档替换 |
| 既有工具实现 | 除本轮 MDK 插件修复外，芯片/map 工具、MDK PowerShell 与安装器保持原版本 |

重新打开并核对 [OpenAI 官方技能文档](https://learn.chatgpt.com/docs/build-skills)：文档链接可访问，Codex 本地技能路径及隐式调用默认值与本项目说明一致。

检查输出、基线与逐文件哈希保存在维护者本地发布检查归档；本页不是公开 CI 链接。发布提交完成后核对 Git 差异与工作区，发布包应从该提交的跟踪文件生成，避免打包本地配置、日志和缓存。

## 发布判断与未覆盖范围

本轮离线检查通过，未发现本轮修改的未解决阻塞项。可作为技能、文档与工具更新的发布候选；不能据此宣称新版本已通过真实设备或所有宿主版本验证。

- 未运行真实 DSH 会话加载、Codex 独立行为评估、UV4 构建或烧录。正式发布 DSH 工作台前，在实际使用版本上确认三个技能可见，`/build` 与 `/flash` 正确注册；若宣称编译/烧录链路实测可用，再补对应工程与目标测试。
- MDK 回归使用替身进程验证调用、时限和错误报告，没有复现真实的 15 分钟超时，也不能证明外层中断时所有后端子进程已退出。中断后需核对实际状态。
- DSH `-Symlink` 安装未测试；本轮只验证复制模式。未覆盖其它 Python / Node / DSH 版本或跨平台矩阵。
- 状态图检查不验证 FreeRTOS 调度、DMA/cache、FPGA CDC、物理安全和长期可靠性。实际工程验证仍按项目需求执行。

本次仅准备本地提交和检查记录；版本号、标签、远端推送与发布由维护者决定。
