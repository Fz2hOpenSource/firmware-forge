# 2026-09-10 技能与项目文档整合

> 本文记录该日整合过程。项目当前定位（嵌入式技能、命令与工具；Codex / DSH 两种入口）以最新 [README](../README.md) 与 [发布前检查](release-check-2026-09-10.md) 为准。

本次以 Firmware Forge 的 `c825a57` 为基线，选择性合入前期维护副本 `886ab89` 中的技能规则、状态检查器与开源文档。没有合并另一仓库的历史，也没有覆盖本项目较新的工具实现。

## 范围

- 三个技能协作遵循需求先行、最小足够实现、真实等待有界退出和局部扩展。补充简单启停、多通道、未知结果与迟到回调的决策依据。
- 测试技能提供可选的有限状态图检查器、严格门禁、示例和测试；没有新增通用固件状态机框架。
- 补充设计理念、贡献与 AI 协作约定、行为验收场景和中英文使用入口。MDK 文档保留本项目 `/build`、`/flash` 分离的行为。
- 保留芯片查询、map 分析、MDK 脚本、插件、安装脚本和批处理入口。DSH YAML 只调整 persona 文本与说明注释，保留服务及 realm 配置。

## 验证范围

在 Windows / Python 3.14.2 的隔离暂存副本执行，共 15 组命令检查通过：

- 三技能格式校验通过；仓库校验器 0 失败、0 警告。
- 状态检查器 18 项测试、当前芯片查询工具 23 项测试通过。后者使用构造数据库，不代表全部真实 CubeMX 版本。
- 两个状态图命令分别得到预期的成功和恢复环失败；两类工具帮助命令正常。
- MDK 插件语法、命令注册、旧烧录开关拒绝行为通过；PowerShell 仅解析，未执行安装或烧录脚本。
- 核对 108 个本地文档链接、5 份 YAML；解析 JS 标签但不执行。对比确认 persona 之外的 YAML 数据一致，29 个既有实现及配置文件逐字节保留。
- 中英文首次安装示例一致，并在隔离目录验证完整复制和已有定制副本时拒绝覆盖；真实用户技能目录未改动。

检查输出和逐文件哈希保留在维护者本地迁移归档，本文是结果摘要，不是公开 CI 链接。文档记录完成后另核对最终差异和新增链接。未运行真实 DSH 会话、MDK 构建、烧录、目标固件或独立 AI 行为评估。

历史审查另见 [2026-09-08 审查记录](skills-audit-2026-09-08.md)，其结果只适用于当时的版本。

## 后续维护

本仓库是共用技能与公开文档的维护源。已有宿主安装副本按文件比较、备份与合并，保留专用工具能力，见 [技能维护](skills-integration.md)。前期副本保留为历史备份。

按 [贡献指南](../CONTRIBUTING.md) 完成相关验证及聚焦提交；实际设备接入从一条真实启停或重配路径开始。图检查和文档审查不能证明 FreeRTOS 调度、DMA/cache、FPGA CDC、硬件安全或长期可靠性，也不替代独立 AI 行为评估。
